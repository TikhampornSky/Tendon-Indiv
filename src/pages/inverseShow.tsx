import React from "react"
import { textt } from "../services/InverseGet"
import { lala } from './tmp'

function ShowInverse() {
    return (
        <div>
            <h2> Hello Inversify</h2>
            <p> { lala } </p>
            <p> { textt } </p>
        </div>
    ) 
}

export default ShowInverse